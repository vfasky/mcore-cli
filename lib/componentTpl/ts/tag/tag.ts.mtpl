/**
* [desc]
* @date ${curDate}
* @author 
* @link 
*/

import * as mcore from 'mcore3'
import Base from 'tag/base'

export default class ${ComponentName} extends Base {

    init () {
        this.render(require('./${componentName}.tpl'))
    }
    
}

mcore.Template.components['${componentCssName}'] = ${ComponentName}